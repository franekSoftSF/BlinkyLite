<Project Sdk="Microsoft.NET.Sdk">

  <!-- Plain net10.0: the server builds and runs on Linux, and everything it
       shares with the workstation lives here. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

</Project>
