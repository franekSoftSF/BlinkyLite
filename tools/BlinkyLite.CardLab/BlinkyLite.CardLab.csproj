<Project Sdk="Microsoft.NET.Sdk">

  <!-- A bench tool, not part of the product: it is how the issuance engine is
       driven against a real token before the WPF client exists (0023). Not in
       any installer. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <IsPackable>false</IsPackable>
    <InvariantGlobalization>false</InvariantGlobalization>
    <RootNamespace>BlinkyLite.CardLab</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\BlinkyLite.Issuance\BlinkyLite.Issuance.csproj" />
  </ItemGroup>

</Project>
